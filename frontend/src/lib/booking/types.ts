// The booking flow's data types now come straight from the public API client
// (@/lib/public/types). What remains here is the small bit of UI-only vocabulary the wizard
// steps share.

export type AuthMode = "login" | "register";
