NODE_ENV=development
PORT=3000

DATABASE_URL=postgresql://agendasaas:agendasaas@localhost:5432/agendasaas

JWT_ACCESS_SECRET=change-me-access-secret
JWT_ACCESS_EXPIRES_IN=15m

REFRESH_TOKEN_EXPIRES_IN_DAYS=30

SEED_PLATFORM_ADMIN_EMAIL=admin@agendasaas.local
SEED_PLATFORM_ADMIN_PASSWORD=ChangeMe123!

# All optional — unset means WhatsApp/E-mail run in log-only mode and the payment gateway
# runs in sandbox mode (see HttpWhatsAppNotifier/HttpEmailNotifier/HttpPaymentGatewayAdapter).
# NOTIFICATIONS_WHATSAPP_WEBHOOK_URL=
# NOTIFICATIONS_WHATSAPP_API_TOKEN=
# NOTIFICATIONS_EMAIL_WEBHOOK_URL=
# NOTIFICATIONS_EMAIL_API_TOKEN=
# PAYMENT_GATEWAY_API_URL=
# PAYMENT_GATEWAY_API_TOKEN=
# Required once PAYMENT_GATEWAY_API_URL is set — authenticates POST /payments/webhook.
# PAYMENT_GATEWAY_WEBHOOK_SECRET=
